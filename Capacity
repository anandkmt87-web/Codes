#include<stdio.h>
int main()
{
    int T, S, B;
    long long bytes;
    scanf("%d %d %d",&T,&S,&B);
    bytes = 2LL * T * S * B * 512;
    long long capacity = bytes / 1024;
    printf("%lld KB",capacity);
    return 0;
}
